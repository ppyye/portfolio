import {Tag} from "@chakra-ui/react";

function CertificateTag() {
    return (
        <Tag size='md' fontWeight={700} px={2} mr={1} borderRadius='full' variant='subtle' colorScheme='orange'>
            자격
        </Tag>
    );
}

export default CertificateTag;