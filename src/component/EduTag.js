import {Tag} from "@chakra-ui/react";

function EduTag() {
    return (
        <Tag size='lg' fontWeight={700} px={2} mr={1} pb={0.5} borderRadius='full' variant='subtle' colorScheme='green'>
            교육
        </Tag>
    );
}

export default EduTag;